// ARRAYS
// Ordered collections of values.
// List of comments on IG post
// Collection of levels in a game
// Songs in a playlist

let fruits = ["orange", "mango", "banana"];
console.log(fruits);
let mixed = ["hello", 23, true, undefined, null];
mixed[0] = "Changed"; // modifying array elements
console.log(mixed.length); // Returns total number of elements within the array
console.log(mixed[0]); // Returns the element at the specified index
console.log(mixed[5]); //undefined
console.log(mixed);
