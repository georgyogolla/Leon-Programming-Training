// FUNCTION
// EXPRESSIONS
// There's another syntax we can use to
// define functions:

const sum = function (a, b) {
  return a + b;
};
const res = sum(40, 50);
console.log(res);
// console.log(sum(40, 50));
